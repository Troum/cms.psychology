# Настройка коллекции Links с Lucide Icons

## ✅ Что было сделано

1. **Установлена библиотека** `lucide-react` для иконок
2. **Создан компонент** `IconPicker` - визуальный выбор иконок в админ-панели
3. **Обновлена коллекция** `Links` с полями:
   - `title` - название ссылки
   - `url` - URL, email или телефон с валидацией (`https://...`, `mailto:...`, `tel:...`)
   - `icon` - выбор иконки через визуальный интерфейс
   - `description` - описание (опционально)
   - `isActive` - активность ссылки
   - `order` - порядок сортировки
4. **Автоматические REST API endpoints** от Payload CMS для работы с ссылками

## 🚀 Как использовать

### В админ-панели

1. **Перезапустите проект** (если он уже был запущен): 
   ```bash
   # Остановите текущий процесс (Ctrl+C)
   pnpm dev
   ```
2. Перейдите в админ-панель: `http://localhost:3000/admin`
3. Откройте раздел **"Ссылки"** (Links)
4. Нажмите **"Create New"**
5. Заполните поля:
   - **Название**: например, "Facebook", "Email", "Телефон"
   - **Ссылка**: 
     - Для сайтов: `https://facebook.com/mypage`
     - Для email: `mailto:info@example.com`
     - Для телефона: `tel:+1234567890`
   - **Иконка**: выберите из визуального списка или используйте поиск
   - **Описание**: опционально
   - **Порядок сортировки**: 0, 1, 2... (меньшие числа отображаются первыми)
6. Сохраните

### Получение данных через REST API

```typescript
// В клиентском компоненте
const response = await fetch('/api/links?where[isActive][equals]=true&sort=order')
const data = await response.json()
// data.docs содержит массив ссылок
```

**Доступные endpoints:**
- `GET /api/links` - получить все ссылки
- `GET /api/links/:id` - получить одну ссылку
- `POST /api/links` - создать (требует авторизации)
- `PATCH /api/links/:id` - обновить (требует авторизации)
- `DELETE /api/links/:id` - удалить (требует авторизации)

## 📦 Доступные иконки

Компонент включает 50+ популярных иконок:

- **Социальные сети**: Facebook, Twitter, Instagram, LinkedIn, YouTube, TikTok, Twitch
- **Мессенджеры**: WhatsApp, Telegram, Discord, Slack, MessageCircle
- **Профессиональные**: GitHub, GitLab, Dribbble, Figma
- **Контакты**: Mail, Phone, Globe, AtSign
- **Другие**: Link, Share2, ExternalLink, Hash, RSS, Podcast, Music, Video

Полный список: [lucide.dev/icons](https://lucide.dev/icons/)

## 🎨 Кастомизация

### Добавление новых иконок

Откройте `src/components/IconPickerClient.tsx` и добавьте название иконки в массив `socialIcons`:

```typescript
const socialIcons = [
  'Facebook',
  'Twitter',
  // ... существующие
  'YourNewIcon', // ← добавьте здесь
]
```

Все доступные иконки можно посмотреть на [lucide.dev/icons](https://lucide.dev/icons/)

## 📁 Структура файлов

```
src/
├── collections/
│   └── Links.ts                    # Конфигурация коллекции
├── components/
│   ├── IconPickerField.tsx         # Обертка для компонента выбора иконок
│   ├── IconPickerClient.tsx        # Клиентский компонент выбора иконок
│   └── README_LINKS.md             # Подробная документация
└── app/
    └── (payload)/
        └── admin/
            └── importMap.js        # Регистрация кастомных компонентов
```

**Примечание:** API endpoints создаются автоматически Payload CMS. Фронтенд-компоненты не включены, так как данные используются через REST API.

## 🔧 Устранение неполадок

### Иконки не отображаются в админ-панели

1. Проверьте, что `lucide-react` установлен: `pnpm list lucide-react`
2. Убедитесь, что компонент добавлен в `importMap.js`
3. Перезапустите dev-сервер

### Не получается получить ссылки через API

1. Убедитесь, что создали хотя бы одну активную ссылку
2. Проверьте, что поле `isActive` установлено в `true`
3. Проверьте URL запроса: `/api/links?where[isActive][equals]=true&sort=order`
4. Убедитесь, что сервер запущен

## 📚 Дополнительная информация

Подробная документация: `src/components/README_LINKS.md`

---

**Готово!** Теперь вы можете управлять социальными ссылками через удобную админ-панель с визуальным выбором иконок! 🎉

